"use strict";

// Classic-script compatibility declarations for the Requirements 11–20 completion layer.
// That layer replaces these hooks with its accounting-aware implementations.
var openCustomerPayment;
var openSupplierPayment;
