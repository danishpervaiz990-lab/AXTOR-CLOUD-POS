"use strict";

/* Production Add New Product form. Reuses generic Product + Grocery profile APIs. */
function g7UomRow(unit="",multiplier="",price=""){return `<div class="g7-uom-row"><div class="field"><label>UOM</label><input data-g7-uom="unit" value="${esc(unit)}" placeholder="PACK"></div><div class="field"><label>Base multiplier</label><input data-g7-uom="multiplier" inputmode="decimal" value="${esc(multiplier)}" placeholder="6"></div><div class="field"><label>Optional price</label><input data-g7-uom="price" inputmode="decimal" value="${esc(price)}"></div><button type="button" class="button button-secondary" data-g7-remove-uom>Remove</button></div>`;}
function g7ReadUoms(form,baseUnit){const rows=[...form.querySelectorAll(".g7-uom-row")].map(row=>{const unit=String(row.querySelector('[data-g7-uom="unit"]')?.value||"").trim().toUpperCase(),multiplier=Math.max(.0001,num(row.querySelector('[data-g7-uom="multiplier"]')?.value,1)),priceText=String(row.querySelector('[data-g7-uom="price"]')?.value||"").trim();return unit?{unit,multiplier,...(priceText?{price:Math.max(0,num(priceText))}:{})}:null;}).filter(Boolean);if(!rows.some(x=>x.unit===baseUnit))rows.unshift({unit:baseUnit,multiplier:1});return rows;}
function g7Csv(value){return String(value||"").split(/[,\n]+/).map(x=>x.trim()).filter(Boolean);}

async function g7ProductPreview(host,input){try{const d=unwrap(await get("/api/v1/grocery/enhancement/numbering/product/preview"));host.textContent=d.preview||"Auto on save";if(input&&!input.value)input.placeholder=d.preview||"PRD-000001";}catch(_){host.textContent="Auto on save";}}

async function g7AddProduct(){
  loading("Add New Product","Grocery product master with barcode, PLU, UOM, pricing, stock and expiry controls");
  try{
    const [catalog,supplierPayload,warehousePayload]=await Promise.all([
      GroceryEnhancementCore.ensureCatalog().catch(()=>({})),
      get("/api/v1/suppliers?limit=500").catch(()=>({data:[]})),
      get("/api/v1/inventory/warehouses").catch(()=>({data:[]})),
    ]);
    const suppliers=rows(supplierPayload,["suppliers","items"]),warehouses=rows(warehousePayload,["warehouses","items"]),categories=state.context?.categories||[];
    app.innerHTML=shell(`<section class="panel"><div class="panel-head"><div><h2>Add Grocery Product</h2><p class="muted">Creates a real tenant product and then validates Grocery-specific barcode/UOM rules. Auto allocation is server controlled.</p></div><button class="button button-secondary" data-nav="inventory">Product / Stock List</button></div><form id="g7-product-form" class="form-grid form-two"><div class="field"><label>Product name *</label><input name="name" required maxlength="180"></div><div class="field"><label>Category</label>${categories.length?`<select name="category"><option value="">Select category</option>${categories.map(x=>`<option value="${esc(x.name||x.id)}">${esc(x.name||x.code||x.id)}</option>`).join("")}</select>`:'<input name="category">'}</div><div class="span-two g7-card"><div class="g7-inline"><label class="check"><input type="checkbox" name="autoCode" checked> Auto Generate Product Code / SKU</label><div id="g7-product-code-preview" class="g7-code-preview">Loading…</div></div><div class="field"><label>Manual Product Code / SKU</label><input name="sku" autocomplete="off" placeholder="Used only when Auto Generate is off"><small>Manual values are uniqueness-checked by the backend.</small></div></div><div class="field"><label>Primary barcode</label><input name="barcode" inputmode="numeric" autocomplete="off"></div><div class="field"><label>PLU</label><input name="plu" autocomplete="off"></div><div class="field span-two"><label>Secondary barcodes</label><input name="barcodes" placeholder="Separate with commas"></div><div class="field"><label>Base unit / UOM *</label><select name="baseUnit" required><option>PCS</option><option>PACK</option><option>CARTON</option><option>KG</option><option>GRAM</option><option>LITRE</option><option>ML</option><option>BOX</option><option>CASE</option></select></div><div class="field"><label>Primary supplier</label><select name="supplierId"><option value="">None</option>${suppliers.map(x=>`<option value="${esc(x.id)}">${esc(x.name||x.displayName||x.code||x.id)}</option>`).join("")}</select></div><div class="field"><label>Purchase / cost price *</label><input name="costPrice" inputmode="decimal" required value="0"></div><div class="field"><label>Retail selling price *</label><input name="retailPrice" inputmode="decimal" required value="0"></div><div class="field"><label>Wholesale price</label><input name="wholesalePrice" inputmode="decimal" value="0"></div><div class="field"><label>Member price</label><input name="memberPrice" inputmode="decimal" value="0"></div><div class="field"><label>Promotional price</label><input name="promotionalPrice" inputmode="decimal" value="0"></div><div class="field"><label>Minimum selling price</label><input name="minimumSellingPrice" inputmode="decimal" value="0"></div><div class="field"><label>Tax rate %</label><input name="taxRate" inputmode="decimal" value="0"></div><div class="field"><label>Reorder level</label><input name="reorderLevel" inputmode="decimal" value="0"></div><div class="field"><label>Reorder quantity</label><input name="reorderQuantity" inputmode="decimal" value="0"></div><div class="field"><label>Maximum stock</label><input name="maxStock" inputmode="decimal" value="0"></div><div class="field"><label>Opening stock</label><input name="openingStock" inputmode="decimal" value="0"></div><div class="field"><label>Opening/default warehouse</label><select name="warehouseId"><option value="">Tenant default</option>${warehouses.map(x=>`<option value="${esc(x.id)}">${esc(x.name||x.code||x.id)}</option>`).join("")}</select></div><div class="field"><label>Brand</label><input name="brand"></div><div class="span-two"><div class="panel-head"><div><h3>Alternate UOMs</h3><p class="muted">Example: PACK × 6 PCS; optional pack selling price.</p></div><button type="button" id="g7-add-uom" class="button button-secondary">+ UOM</button></div><div id="g7-uoms">${g7UomRow("PCS","1","")}</div></div><div class="span-two g7-check-grid"><label class="check"><input type="checkbox" name="weightedBarcode"> Weighted item / embedded weight barcode</label><label class="check"><input type="checkbox" name="priceEmbeddedBarcode"> Price-embedded barcode</label><label class="check"><input type="checkbox" name="batchTracking"> Batch tracking</label><label class="check"><input type="checkbox" name="expiryTracking"> Expiry tracking</label><label class="check"><input type="checkbox" name="active" checked> Active product</label></div><div class="field"><label>Weighted barcode prefix</label><input name="weightedBarcodePrefix" inputmode="numeric" minlength="2" maxlength="8" placeholder="2–8 digits"></div><div class="field"><label>Price-embedded prefix</label><input name="priceEmbeddedBarcodePrefix" inputmode="numeric" minlength="2" maxlength="8" placeholder="2–8 digits"></div><div class="span-two g7-submit-row"><button type="button" class="button button-secondary" data-nav="inventory">Cancel</button><button class="button button-primary">Create Product</button></div></form></section>`,"Add New Product",`${(catalog?.numbering||[]).length?"Tenant numbering ready":"Server numbering"} · ${suppliers.length} suppliers`);
    bindShell();
    const form=document.getElementById("g7-product-form"),preview=document.getElementById("g7-product-code-preview"),sku=form.elements.sku;
    g7ProductPreview(preview,sku);
    document.getElementById("g7-add-uom")?.addEventListener("click",()=>document.getElementById("g7-uoms")?.insertAdjacentHTML("beforeend",g7UomRow()));
    document.getElementById("g7-uoms")?.addEventListener("click",e=>{const b=e.target.closest?.("[data-g7-remove-uom]");if(b&&document.querySelectorAll(".g7-uom-row").length>1)b.closest(".g7-uom-row")?.remove();});
    form.elements.baseUnit.addEventListener("change",()=>{const first=form.querySelector('.g7-uom-row [data-g7-uom="unit"]');if(first)first.value=form.elements.baseUnit.value;});
    form.elements.autoCode.addEventListener("change",()=>{sku.disabled=form.elements.autoCode.checked;if(!form.elements.autoCode.checked)sku.focus();});sku.disabled=true;
    form.addEventListener("submit",async e=>{
      e.preventDefault();form.querySelectorAll(".notice-error,.notice-ok").forEach(x=>x.remove());const o=fdObj(form),baseUnit=String(o.baseUnit||"PCS").toUpperCase(),auto=form.elements.autoCode.checked;let createdId=null;
      try{
        let code=String(o.sku||"").trim();if(auto){const allocation=unwrap(await post("/api/v1/grocery/enhancement/numbering/product/allocate",{}));code=String(allocation.value||"").trim();preview.textContent=code;}if(!code)throw new Error("Product Code / SKU is required when Auto Generate is disabled.");
        const profile={barcodes:g7Csv(o.barcodes),plu:o.plu||null,baseUnit,uoms:g7ReadUoms(form,baseUnit),weightedBarcode:form.elements.weightedBarcode.checked,weightedBarcodePrefix:o.weightedBarcodePrefix||null,priceEmbeddedBarcode:form.elements.priceEmbeddedBarcode.checked,priceEmbeddedBarcodePrefix:o.priceEmbeddedBarcodePrefix||null,batchTracking:form.elements.batchTracking.checked,expiryTracking:form.elements.expiryTracking.checked,retailPrice:Math.max(0,num(o.retailPrice)),wholesalePrice:Math.max(0,num(o.wholesalePrice)),memberPrice:Math.max(0,num(o.memberPrice)),promotionalPrice:Math.max(0,num(o.promotionalPrice)),minimumSellingPrice:Math.max(0,num(o.minimumSellingPrice)),reorderLevel:Math.max(0,num(o.reorderLevel)),reorderQuantity:Math.max(0,num(o.reorderQuantity)),maxStock:Math.max(0,num(o.maxStock))};
        const customFields={primarySupplierId:o.supplierId||null,defaultWarehouseId:o.warehouseId||null,taxRate:Math.max(0,num(o.taxRate)),grocery:profile,plu:profile.plu,barcodes:profile.barcodes,uomConversions:profile.uoms.filter(x=>x.unit!==baseUnit).map(x=>({unit:x.unit,factor:x.multiplier,multiplier:x.multiplier,...(x.price!=null?{price:x.price}:{})})),weightedBarcode:profile.weightedBarcode,weightedProduct:profile.weightedBarcode,weightedBarcodePrefix:profile.weightedBarcodePrefix,priceEmbeddedBarcode:profile.priceEmbeddedBarcode,priceEmbeddedBarcodePrefix:profile.priceEmbeddedBarcodePrefix,batchTracking:profile.batchTracking,expiryTracking:profile.expiryTracking};
        const raw=await post("/api/v1/products",{name:String(o.name||"").trim(),sku:code,code,itemCode:code,productCode:code,barcode:String(o.barcode||"").trim()||undefined,category:o.category||undefined,brand:o.brand||undefined,unit:baseUnit,costPrice:Math.max(0,num(o.costPrice)),price:profile.retailPrice,minStock:profile.reorderLevel,openingStock:Math.max(0,num(o.openingStock)),currentStock:Math.max(0,num(o.openingStock)),active:form.elements.active.checked,customFields});
        const product=unwrap(raw)?.product||unwrap(raw);createdId=product?.id;if(!createdId)throw new Error("Product was created but the backend did not return its id.");
        await patch(`/api/v1/grocery/products/${encodeURIComponent(createdId)}/profile`,profile);
        form.insertAdjacentHTML("afterbegin",notice(`Product ${code} created successfully.`));
        setTimeout(()=>navigate("inventory"),500);
      }catch(x){if(createdId){try{await del(`/api/v1/products/${encodeURIComponent(createdId)}`);}catch(_){}}form.insertAdjacentHTML("afterbegin",notice(x.message,true));}
    });
  }catch(e){renderError("Add New Product",e);}
}

window.GroceryEnhancementProduct={render:g7AddProduct};
