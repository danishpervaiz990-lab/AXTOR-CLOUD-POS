async function checkout(){loading("Grocery Sale Terminal","Barcode, PLU, weighted items, split tender, credit and held sales");try{await loadCheckoutContext();if(!state.checkoutBranch)state.checkoutBranch=branches()[0]?.id||"";if(!state.checkoutWarehouse)state.checkoutWarehouse=currentWarehouses()[0]?.id||"";if(!state.checkoutCounter)state.checkoutCounter=currentCounters()[0]?.id||"";renderCheckout();}catch(e){renderError("Grocery Sale Terminal",e);}}
function renderCheckout(){const t=cartTotals(),b=currentBranch(),ware=currentWarehouses(),counters=currentCounters(),salesmen=state.context?.salesmen||[],cats=state.context?.categories||[];app.innerHTML=shell(`<div id="terminal-notice"></div><section class="terminal terminal-wide"><div class="panel"><div class="context-grid"><div class="field"><label>Branch</label><select id="branch-select">${branches().map(x=>`<option value="${esc(x.id)}" ${x.id===b?'selected':''}>${esc(x.name)}</option>`).join("")}</select></div><div class="field"><label>Warehouse</label><select id="warehouse-select">${ware.map(x=>`<option value="${esc(x.id)}" ${x.id===state.checkoutWarehouse?'selected':''}>${esc(x.name)}</option>`).join("")}</select></div><div class="field"><label>Counter</label><select id="counter-select">${counters.map(x=>`<option value="${esc(x.id)}" ${x.id===state.checkoutCounter?'selected':''}>${esc(x.name)}</option>`).join("")}</select></div><div class="field"><label>Salesperson</label><select id="salesperson-select"><option value="">None</option>${salesmen.map(x=>`<option value="${esc(x.id)}" ${x.id===state.salespersonId?'selected':''}>${esc(x.name)}</option>`).join("")}</select></div></div>${state.shift?notice(`Cashier shift open · ${userName()}`):`<div class="shift-alert"><div class="field"><label>Opening cash</label><input id="opening-cash" value="0.00"></div><button id="open-shift" class="button button-primary">Open cashier shift</button></div>`}<div class="field"><label>Scan barcode / SKU / UPC / EAN / PLU / name <small>F2</small></label><div class="search-row"><input id="product-query" autofocus autocomplete="off"><button id="product-search" class="button button-primary">Find</button></div></div><div class="category-strip"><button class="chip" data-category="">All</button>${cats.slice(0,14).map(x=>`<button class="chip" data-category="${esc(x.name)}">${esc(x.name)}</button>`).join("")}</div><div id="product-results" class="product-grid">${state.productResults.map(productCard).join("")}</div><div class="panel inset"><div class="panel-head"><h3>Customer <small>F4</small></h3><button id="instant-customer" class="button button-secondary">+ New</button></div><div class="search-row"><input id="customer-query" value="${esc(state.selectedCustomer?.name||'')}" placeholder="Name, phone or code"><button id="customer-search" class="button button-secondary">Search</button></div><div id="customer-results" class="search-results">${state.customerResults.map(x=>`<button class="result-button" data-select-customer="${esc(x.id)}"><span><strong>${esc(x.name)}</strong><small>${esc(x.phone||x.code||'')}</small></span><span>${money(x.balance)}</span></button>`).join("")}</div>${state.selectedCustomer?`<div class="notice-ok customer-credit"><strong>${esc(state.selectedCustomer.name)}</strong> · Balance ${money(state.selectedCustomer.balance)} · Limit ${money(state.selectedCustomer.creditLimit)} · ${esc(state.selectedCustomer.creditDays||0)} days</div>`:""}</div><div class="panel inset"><div class="panel-head"><h3>Quick products</h3><span class="muted">Favorites / recent</span></div><div class="quick-products">${state.recentProducts.slice(0,8).map(productCard).join("")}</div></div></div><div class="panel cart-panel"><div class="cart-head"><div><span class="eyebrow">Current cart</span><h2>${state.cart.length} lines</h2></div><div class="cart-total">${money(t.total)}</div></div><div class="cart-list">${state.cart.length?state.cart.map(cartLine).join(""):`<div class="empty">Scan or search a product to begin.</div>`}</div><div class="discount-grid"><div class="field"><label>Invoice discount</label><input id="invoice-discount" value="${esc(state.invoiceDiscount)}" inputmode="decimal"></div><div class="field"><label>Discount type</label><select id="invoice-discount-type"><option value="fixed" ${state.invoiceDiscountType==='fixed'?'selected':''}>Fixed</option><option value="percent" ${state.invoiceDiscountType==='percent'?'selected':''}>Percent</option></select></div><div class="field"><label>Promotional discount</label><input id="promotion-discount" value="${esc(state.promotionDiscount||0)}" inputmode="decimal"></div><div class="field"><label>Promotion status</label><input value="Manual terminal promotion; automatic rules arrive with Promotion Engine" disabled></div></div><div class="panel-head"><h3>Split tender <small>F8</small></h3><button id="add-payment" class="button button-secondary">+ Payment</button></div><div class="payment-list">${state.payments.map(paymentRow).join("")}</div><div class="totals"><div class="total-row"><span>Subtotal</span><strong>${money(t.subtotal)}</strong></div><div class="total-row"><span>Line discounts</span><strong>-${money(t.lineDiscount)}</strong></div><div class="total-row"><span>Invoice discount</span><strong>-${money(t.invoiceDiscount)}</strong></div><div class="total-row"><span>Promotional discount</span><strong>-${money(t.promotionDiscount)}</strong></div><div class="total-row"><span>Tax</span><strong>${money(t.tax)}</strong></div><div class="total-row"><span>Rounding</span><strong>${money(t.rounding)}</strong></div><div class="total-row grand"><span>Total</span><strong>${money(t.total)}</strong></div><div class="total-row"><span>Tendered</span><strong>${money(t.tendered)}</strong></div><div class="total-row"><span>Applied / paid</span><strong>${money(t.paid)}</strong></div><div class="total-row"><span>Change</span><strong>${money(t.change)}</strong></div><div class="total-row grand"><span>Balance / credit</span><strong>${money(t.balance)}</strong></div></div><div class="field"><label>Credit due date</label><input id="sale-due" type="date" value="${esc(state.saleDueDate||'')}"></div>${t.balance>0&&can("sales_documents.override_credit_limit")?`<div class="field"><label>Override reason (required only when policy is exceeded)</label><input id="credit-override" value="${esc(state.creditOverrideReason)}"></div>`:""}<div class="field"><label>Sale notes</label><textarea id="sale-notes">${esc(state.notes)}</textarea></div><div class="button-grid"><button id="hold-sale" class="button button-secondary" ${state.cart.length?'':'disabled'}>Hold · F6</button><button id="void-cart" class="button button-danger" ${state.cart.length?'':'disabled'}>Void cart</button><button id="complete-sale" class="button button-primary" ${state.cart.length?'':'disabled'}>Complete · F9</button></div></div></section>`,"Grocery Sale Terminal",`${userName()} · ${role()}`);bindShell();bindCheckout();}
