import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/inventory.controller.js";const router=Router();router.use(requireAuth);router.get("/warehouses",c.warehouses);router.post("/warehouses",c.createWarehouse);router.patch("/warehouses/:id",c.updateWarehouse);router.delete("/warehouses/:id",c.deleteWarehouse);router.get("/stock",c.stock);router.get("/movements",c.movements);router.post("/adjustments",c.adjustment);router.post("/transfers",c.transfer);router.get("/valuation",c.valuation);router.get("/low-stock",c.lowStock);router.get("/stock-counts",c.counts);router.get("/stock-counts/sheet/:warehouseId",c.countSheet);router.post("/stock-counts/approve",c.approveCount);export{router};export const inventoryRoutes=router;export default router;
