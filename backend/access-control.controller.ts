import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import{requirePermission}from"../middleware/permission.middleware.js";import * as c from"../controllers/reports.controller.js";const router=Router();router.use(requireAuth,requirePermission("reports.view"));router.get("/options",c.options);router.get("/:reportId",c.run);export{router};export const reportsRoutes=router;export default router;
