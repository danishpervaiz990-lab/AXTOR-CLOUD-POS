[build]
builder = "NIXPACKS"
buildCommand = "npm run prisma:generate && npm run build"

[deploy]
startCommand = "prisma db push --skip-generate --accept-data-loss && npm run seed:catalog && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
