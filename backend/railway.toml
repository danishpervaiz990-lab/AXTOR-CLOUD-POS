[build]
builder = "NIXPACKS"
buildCommand =buildCommand = "npm install --include=dev --no-audit --no-fund && npm run prisma:generate && npm run build"

[deploy]
startCommand = "npm run prisma:deploy && npm run seed:catalog && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
