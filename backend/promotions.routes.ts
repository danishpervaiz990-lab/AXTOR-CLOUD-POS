import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/notifications.controller.js";const router=Router();router.use(requireAuth);router.get("/audit",c.audit);router.post("/read-all",c.readAll);router.get("/",c.list);router.post("/",c.create);router.post("/:id/read",c.read);router.post("/:id/unread",c.unread);router.delete("/:id",c.remove);export{router};export const notificationsRoutes=router;export default router;
