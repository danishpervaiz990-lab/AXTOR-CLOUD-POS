import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import{requirePersistentIdempotency}from"../middleware/idempotency.middleware.js";import * as c from"../controllers/inventory.controller.js";const router=Router();router.use(requireAuth);router.get("/warehouses",c.warehouses);router.post("/warehouses",requirePersistentIdempotency("inventory.warehouse.create"),c.createWarehouse);router.patch("/warehouses/:id",requirePersistentIdempotency("inventory.warehouse.update"),c.updateWarehouse);router.delete("/warehouses/:id",requirePersistentIdempotency("inventory.warehouse.delete"),c.deleteWarehouse);router.get("/stock",c.stock);router.get("/movements",c.movements);router.post("/adjustments",requirePersistentIdempotency("inventory.adjustment.create"),c.adjustment);router.post("/transfers",requirePersistentIdempotency("inventory.transfer.create"),c.transfer);router.get("/valuation",c.valuation);router.get("/low-stock",c.lowStock);router.get("/stock-counts",c.counts);router.get("/stock-counts/sheet/:warehouseId",c.countSheet);router.post("/stock-counts/approve",requirePersistentIdempotency("inventory.stock-count.approve"),c.approveCount);export{router};export const inventoryRoutes=router;export default router;
