import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/promotions.controller.js";const router=Router();router.use(requireAuth);router.post("/validate",c.validate);router.get("/",c.list);router.post("/",c.create);router.patch("/:id",c.update);router.delete("/:id",c.remove);export{router};export const promotionsRoutes=router;export default router;
