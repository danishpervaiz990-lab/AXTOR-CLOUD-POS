import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/accounts.controller.js";const router=Router();router.use(requireAuth);router.get("/transactions",c.transactions);router.post("/transactions",c.addTransaction);router.post("/:id/reconcile",c.reconcile);router.get("/",c.list);router.post("/",c.create);router.get("/:id",c.get);router.patch("/:id",c.update);router.delete("/:id",c.remove);export{router};export const accountsRoutes=router;export default router;
