import type {Request,Response}from"express";import * as s from"../services/communications.service.js";import{handleError,tenant}from"../utils/http.js";const go=(fn:any)=>(async(req:Request,res:Response)=>{try{const t=tenant(req);res.json({ok:true,data:await fn(req,t)});}catch(e){handleError(res,e);}});export const list=go((r:any,t:any)=>s.listCommunications(t.businessId,r.query));export const create=go((r:any,t:any)=>s.createCommunication(r,t.businessId,t.userId,r.body));export const status=go((r:any,t:any)=>s.updateStatus(r,t.businessId,t.userId,r.params.id,r.body));
