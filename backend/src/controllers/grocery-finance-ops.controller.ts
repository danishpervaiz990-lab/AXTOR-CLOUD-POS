import type { Request, Response } from "express";
import { prisma } from "../db/prisma.js";
import { writeAudit } from "../services/audit.service.js";
import { nextEntityNumber } from "../services/numbering.service.js";
import { ensureStandardGroceryAccounts, postBalancedGroceryJournal, postGroceryCustomerPaymentAccounting, postGroceryExpenseAccounting, postGrocerySaleAccounting, postGrocerySupplierPaymentAccounting } from "../services/grocery-accounting.service.js";

const db:any=prisma; const DAY=86_400_000;
function text(v:unknown){return String(v??"").trim();} function num(v:unknown,f=0){const n=Number(v);return Number.isFinite(n)?n:f;} function round2(v:number){return Math.round((v+Number.EPSILON)*100)/100;} function json(v:unknown):Record<string,any>{return v&&typeof v==="object"&&!Array.isArray(v)?v as Record<string,any>:{};} function asDate(v:unknown){const d=new Date(String(v||""));return Number.isNaN(d.getTime())?null:d;} function tenant(req:Request){const businessId=req.tenant?.businessId,userId=req.tenant?.userId;if(!businessId||!userId)throw new Error("Authenticated Grocery tenant is required");return{businessId,userId};} function ok(res:Response,data:unknown,status=200){return res.status(status).json({ok:true,data});} function fail(res:Response,e:any,status=400){return res.status(status).json({ok:false,error:{message:e?.message||"Request failed"}});}
function journalTotals(lines:any[]){const debit=round2(lines.reduce((s,x)=>s+Math.max(0,num(x.debit)),0)),credit=round2(lines.reduce((s,x)=>s+Math.max(0,num(x.credit)),0));return{debit,credit,balanced:Math.abs(debit-credit)<=.001};}

export async function groceryChartOfAccounts(req:Request,res:Response){try{const t=tenant(req),rows=await db.$transaction(async(tx:any)=>{await ensureStandardGroceryAccounts(tx,t.businessId);return tx.account.findMany({where:{businessId:t.businessId,active:true},orderBy:[{type:"asc"},{accountNumber:"asc"},{name:"asc"}]});});return ok(res,rows);}catch(e){return fail(res,e);}}
export async function bootstrapGroceryAccounting(req:Request,res:Response){try{const t=tenant(req),rows=await db.$transaction((tx:any)=>ensureStandardGroceryAccounts(tx,t.businessId));await writeAudit(db,req,{businessId:t.businessId,userId:t.userId,action:"grocery.accounting.bootstrap",entityType:"Account",after:{count:rows.length}});return ok(res,rows);}catch(e){return fail(res,e);}}
export async function groceryJournals(req:Request,res:Response){try{const t=tenant(req);return ok(res,await db.industryRecord.findMany({where:{businessId:t.businessId,industryCode:"grocery",entityType:"grocery_journal_entry",archivedAt:null},orderBy:{createdAt:"desc"},take:500}));}catch(e){return fail(res,e);}}
export async function createGroceryJournal(req:Request,res:Response){try{const t=tenant(req),lines=Array.isArray(req.body?.lines)?req.body.lines:[],totals=journalTotals(lines);if(lines.length<2||!totals.balanced)throw new Error(`Journal must balance. Debit ${totals.debit.toFixed(2)} / Credit ${totals.credit.toFixed(2)}`);const manualReference=text(req.body?.journalNo);const row=await db.$transaction(async(tx:any)=>{let referenceNo=manualReference;if(referenceNo){const duplicate=await tx.industryRecord.findFirst({where:{businessId:t.businessId,industryCode:"grocery",entityType:"grocery_journal_entry",referenceNo,archivedAt:null}});if(duplicate)throw new Error("Journal number already exists");}else{referenceNo=await nextEntityNumber(tx,"industryRecord","referenceNo",t.businessId,"JRN",6,{sequenceKey:"grocery.journal",where:{industryCode:"grocery",entityType:"grocery_journal_entry"}});}const created=await tx.industryRecord.create({data:{businessId:t.businessId,industryCode:"grocery",entityType:"grocery_journal_entry",referenceNo,displayName:text(req.body?.description)||referenceNo,status:"DRAFT",startAt:asDate(req.body?.date)||new Date(),data:{reference:text(req.body?.reference)||null,branchId:text(req.body?.branchId)||null,warehouseId:text(req.body?.warehouseId)||null,customerId:text(req.body?.customerId)||null,supplierId:text(req.body?.supplierId)||null,attachment:text(req.body?.attachment)||null,description:text(req.body?.description)||null,lines,totals,createdBy:t.userId},createdByUserId:t.userId,updatedByUserId:t.userId}});await writeAudit(tx,req,{businessId:t.businessId,userId:t.userId,action:"grocery.journal.create",entityType:"Journal",entityId:created.id,after:{referenceNo,totals,autoAllocated:!manualReference}});return created;});return ok(res,row,201);}catch(e){return fail(res,e);}}
export async function transitionGroceryJournal(req:Request,res:Response){try{const t=tenant(req),target=text(req.body?.status).toUpperCase();if(!["APPROVED","POSTED","REVERSED"].includes(target))throw new Error("Invalid journal status");const result=await db.$transaction(async(tx:any)=>{const row=await tx.industryRecord.findFirst({where:{id:req.params.id,businessId:t.businessId,industryCode:"grocery",entityType:"grocery_journal_entry",archivedAt:null}});if(!row)throw new Error("Journal not found");const d=json(row.data),lines=Array.isArray(d.lines)?d.lines:[],totals=journalTotals(lines);if(!totals.balanced)throw new Error("Unbalanced journal cannot be posted");if(target==="APPROVED"){if(String(row.status).toUpperCase()!=="DRAFT")throw new Error("Only Draft journals can be approved");d.approvedBy=t.userId;d.approvedAt=new Date().toISOString();}if(target==="POSTED"){if(!["DRAFT","APPROVED"].includes(String(row.status).toUpperCase()))throw new Error("Only Draft/Approved journals can be posted");await postBalancedGroceryJournal(tx,{businessId:t.businessId,userId:t.userId,referenceNo:row.referenceNo||row.id,description:d.description||row.displayName,transactionDate:row.startAt||new Date(),sourceType:"grocery_manual_journal",sourceId:row.id,lines});d.approvedBy=d.approvedBy||t.userId;d.postedBy=t.userId;d.postedAt=new Date().toISOString();}if(target==="REVERSED"){if(String(row.status).toUpperCase()!=="POSTED")throw new Error("Only Posted journals can be reversed");const reverse=lines.map((x:any)=>({...x,debit:num(x.credit),credit:num(x.debit),description:`Reversal: ${text(x.description||d.description)}`}));await postBalancedGroceryJournal(tx,{businessId:t.businessId,userId:t.userId,referenceNo:`${row.referenceNo||row.id}-REV`,description:`Reversal of ${row.referenceNo||row.id}`,transactionDate:new Date(),sourceType:"grocery_manual_journal_reversal",sourceId:row.id,lines:reverse});d.reversedAt=new Date().toISOString();d.reversedBy=t.userId;}const updated=await tx.industryRecord.update({where:{id:row.id},data:{status:target,data:d,revision:{increment:1},updatedByUserId:t.userId}});await writeAudit(tx,req,{businessId:t.businessId,userId:t.userId,action:`grocery.journal.${target.toLowerCase()}`,entityType:"Journal",entityId:row.id,before:{status:row.status},after:{status:target,totals}});return updated;});return ok(res,result);}catch(e){return fail(res,e);}}

export async function groceryExpenses(req:Request,res:Response){try{const t=tenant(req),from=asDate(req.query.from)||new Date(Date.now()-365*DAY),to=asDate(req.query.to)||new Date();to.setHours(23,59,59,999);const rows=await db.expense.findMany({where:{businessId:t.businessId,expenseDate:{gte:from,lte:to}},orderBy:{expenseDate:"desc"},take:5000}),total=round2(rows.reduce((s:number,x:any)=>s+num(x.baseAmount||x.amount),0));return ok(res,{total,rows:rows.map((x:any)=>({...x,amount:num(x.amount),baseAmount:num(x.baseAmount),percentageOfTotal:total===0?0:round2(num(x.baseAmount||x.amount)/total*100),paymentMethod:json(x.metadata).paymentMethod||null,counterId:json(x.metadata).counterId||null,vanId:json(x.metadata).vanId||null}))});}catch(e){return fail(res,e);}}
export async function createGroceryExpense(req:Request,res:Response){try{const t=tenant(req),amount=round2(num(req.body?.amount));if(amount<=0)throw new Error("Expense amount must be greater than zero");const expenseDate=asDate(req.body?.date||req.body?.expenseDate)||new Date(),manualReference=text(req.body?.voucherNo||req.body?.referenceNo),business=await db.business.findUnique({where:{id:t.businessId},select:{currency:true}}),currency=text(req.body?.currency||business?.currency||"QAR").toUpperCase(),exchangeRate=Math.max(.0000001,num(req.body?.exchangeRate,1));const result=await db.$transaction(async(tx:any)=>{let referenceNo=manualReference;if(referenceNo){const duplicate=await tx.expense.findFirst({where:{businessId:t.businessId,referenceNo}});if(duplicate)throw new Error("Expense voucher number already exists");}else{referenceNo=await nextEntityNumber(tx,"expense","referenceNo",t.businessId,"EXP",6,{sequenceKey:"grocery.expense"});}const metadata={paymentMethod:text(req.body?.paymentMethod||"cash"),paymentAccountId:text(req.body?.paymentAccountId)||null,supplierPayee:text(req.body?.supplierPayee)||null,counterId:text(req.body?.counterId)||null,vanId:text(req.body?.vanId)||null,notes:text(req.body?.notes)||null,attachment:text(req.body?.attachment)||null,tax:round2(num(req.body?.tax)),voucherNo:referenceNo};const expense=await tx.expense.create({data:{businessId:t.businessId,accountId:text(req.body?.expenseAccountId)||null,branchId:text(req.body?.branchId)||null,createdByUserId:t.userId,category:text(req.body?.category||"General Expense"),description:text(req.body?.description)||null,amount,currency,exchangeRate,baseAmount:round2(amount*exchangeRate),exchangeRateSource:text(req.body?.exchangeRateSource||"manual"),exchangeRateTimestamp:new Date(),referenceNo,expenseDate,metadata}}),posting=await postGroceryExpenseAccounting(tx,{businessId:t.businessId,userId:t.userId,expense,paymentMethod:metadata.paymentMethod});await writeAudit(tx,req,{businessId:t.businessId,userId:t.userId,action:"grocery.expense.create",entityType:"Expense",entityId:expense.id,after:{referenceNo,amount,category:expense.category,posting,autoAllocated:!manualReference}});return{expense,posting};});return ok(res,result,201);}catch(e){return fail(res,e);}}
export async function groceryExpenseReport(req:Request,res:Response){try{const t=tenant(req),rows=await db.expense.findMany({where:{businessId:t.businessId},orderBy:{expenseDate:"desc"},take:10000}),groupBy=text(req.query.groupBy||"category"),map=new Map<string,number>();for(const x of rows){const m=json(x.metadata),key=groupBy==="paymentMethod"?text(m.paymentMethod||"Unknown"):groupBy==="branch"?text(x.branchId||"Unassigned"):groupBy==="user"?text(x.createdByUserId||"Unknown"):groupBy==="van"?text(m.vanId||"Not Van"):groupBy==="month"?new Date(x.expenseDate).toISOString().slice(0,7):text(x.category||"Uncategorized");map.set(key,(map.get(key)||0)+num(x.baseAmount||x.amount));}const total=round2([...map.values()].reduce((s,x)=>s+x,0));return ok(res,{groupBy,total,rows:[...map].map(([key,amount])=>({key,amount:round2(amount),percentageOfTotal:total===0?0:round2(amount/total*100)})).sort((a,b)=>b.amount-a.amount)});}catch(e){return fail(res,e);}}

export async function reconcileGroceryAccountingSource(req:Request,res:Response){try{const t=tenant(req),type=text(req.params.type).toLowerCase(),id=text(req.params.id),result=await db.$transaction(async(tx:any)=>{if(type==="customer-payment"){const payment=await tx.customerPayment.findFirst({where:{id,businessId:t.businessId}});if(!payment)throw new Error("Customer payment not found");return postGroceryCustomerPaymentAccounting(tx,{businessId:t.businessId,userId:t.userId,payment});}if(type==="supplier-payment"){const payment=await tx.supplierPayment.findFirst({where:{id,businessId:t.businessId}});if(!payment)throw new Error("Supplier payment not found");return postGrocerySupplierPaymentAccounting(tx,{businessId:t.businessId,userId:t.userId,payment});}if(type==="expense"){const expense=await tx.expense.findFirst({where:{id,businessId:t.businessId}});if(!expense)throw new Error("Expense not found");return postGroceryExpenseAccounting(tx,{businessId:t.businessId,userId:t.userId,expense});}if(type==="sale"){const document=await tx.salesDocument.findFirst({where:{id,businessId:t.businessId,documentType:"INVOICE"}});if(!document)throw new Error("Sale not found");const snap=json(json(document.metadata).groceryCostSnapshot);return postGrocerySaleAccounting(tx,{businessId:t.businessId,userId:t.userId,document,cogs:num(snap.totalCogs)});}throw new Error("Unsupported accounting source type");});return ok(res,result);}catch(e){return fail(res,e);}}
