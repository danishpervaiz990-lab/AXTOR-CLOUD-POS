import type {Request,Response}from"express";import * as s from"../services/reports.service.js";import{handleError,tenant}from"../utils/http.js";export async function options(req:Request,res:Response){try{const t=tenant(req);res.json({ok:true,data:await s.options(t.businessId)});}catch(e){handleError(res,e)}}export async function run(req:Request,res:Response){try{const t=tenant(req);res.json({ok:true,data:await s.runReport(t.businessId,req.params.reportId,req.query)});}catch(e){handleError(res,e)}}
