import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/settings.controller.js";const router=Router();router.use(requireAuth);router.get("/export",c.exportAll);router.post("/import",c.importAll);router.put("/bulk",c.bulk);router.get("/",c.list);router.get("/:key",c.get);router.put("/:key",c.set);router.patch("/:key",c.set);router.delete("/:key",c.remove);export{router};export const settingsRoutes=router;export default router;
