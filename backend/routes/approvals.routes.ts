import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/approvals.controller.js";const router=Router();router.use(requireAuth);router.get("/rules",c.rules);router.post("/rules",c.createRule);router.patch("/rules/:id",c.updateRule);router.get("/",c.list);router.post("/",c.create);router.post("/:id/approve",c.approve);router.post("/:id/reject",c.reject);export{router};export const approvalsRoutes=router;export default router;
