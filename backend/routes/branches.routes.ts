import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/branches.controller.js";const router=Router();router.use(requireAuth);router.get("/counters",c.counters);router.post("/counters",c.createCounter);router.patch("/counters/:id",c.updateCounter);router.delete("/counters/:id",c.deleteCounter);router.get("/",c.list);router.post("/",c.create);router.patch("/:id",c.update);router.delete("/:id",c.remove);export{router};export const branchesRoutes=router;export default router;
