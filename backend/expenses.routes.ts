import{Router}from"express";import{requireAuth}from"../middleware/auth.middleware.js";import * as c from"../controllers/loyalty.controller.js";const router=Router();router.use(requireAuth);router.get("/program",c.program);router.put("/program",c.saveProgram);router.get("/accounts",c.accounts);router.get("/ledger",c.ledger);router.post("/earn",c.earn);router.post("/redeem",c.redeem);router.post("/adjust",c.adjust);export{router};export const loyaltyRoutes=router;export default router;
