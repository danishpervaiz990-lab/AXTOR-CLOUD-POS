name: Paint Frontend CI
on:
  pull_request:
    branches: [frontend-paint]
  push:
    branches: [frontend-paint, fix/paint/dedicated-frontend-v1]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: JavaScript syntax
        run: |
          node --check demo-static/js/axtor-api.js
          node --check demo-static/js/paint-app.js
      - name: Paint release suite
        run: node tests/paint-frontend.test.cjs
      - name: Reject generic industry routes
        run: |
          if grep -R "industry.html?module=" demo-static/paint-*.html; then exit 1; fi
      - name: Verify Vercel configuration
        run: node -e "JSON.parse(require('fs').readFileSync('demo-static/vercel.json','utf8')); console.log('Vercel configuration valid')"
