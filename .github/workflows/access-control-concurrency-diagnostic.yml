name: Access Control Concurrency Diagnostic

on:
  pull_request:
    branches: [backend]
    paths:
      - 'backend/src/services/system-roles.service.ts'
      - 'backend/tests/access-control-r13.test.mjs'
      - 'backend/tests/access-control-system-role-concurrency.test.mjs'
      - '.github/workflows/access-control-concurrency-diagnostic.yml'

permissions:
  contents: read

jobs:
  focused:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - run: npm ci --include=dev --no-audit --no-fund
      - run: npx prisma generate
      - run: npm run build
      - name: Run focused access-control tests
        run: node --test tests/access-control-r13.test.mjs tests/access-control-system-role-concurrency.test.mjs
