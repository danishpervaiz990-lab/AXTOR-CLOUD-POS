const fs=require('fs');const assert=require('assert');const html=fs.readFileSync('demo-static/grocery-inventory.html','utf8');const js=fs.readFileSync('demo-static/js/grocery-inventory-integrity.js','utf8');assert(/grocery-inventory-integrity\.js\?v=/.test(html));['/api/v1/inventory/warehouses','/api/v1/inventory/stock','/api/v1/inventory/movements','/api/v1/inventory/transfers','/api/v1/inventory/adjustments','/api/v1/inventory/stock-counts/sheet/','/api/v1/inventory/stock-counts/approve','/api/v1/industry/batches'].forEach(x=>assert(js.includes(x),x));assert(js.includes('Idempotency-Key'));assert(js.includes('Expired / Blocked Batches'));assert(!js.includes('localStorage.setItem'));console.log('Grocery inventory integrity regression passed');